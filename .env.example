# Database connection (optional in development mode - will use in-memory storage)
DATABASE_URL=your_database_url_here

# Image upload API key
IMGBB_API_KEY=your_imgbb_api_key_here

# Environment: 'development' uses in-memory storage, 'production' requires DATABASE_URL
NODE_ENV=development
